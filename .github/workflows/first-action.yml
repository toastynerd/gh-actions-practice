name: "First Workflow"
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "hello world"
      - name: Print goodbye
        run: echo "done, goodbye!"
